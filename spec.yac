%{
#include <stdio.h>
int yylex(void) ;
void yyerror(char *) ;
%}

%token DOSSIER DOSSIERID FLIGHT AIRPORT DATE HOUR OWNER END
%define parse.error verbose

%%
dossier : DOSSIER DOSSIERID OWNER vol vols;
vols : vols vol
     |
     ;
vol: DATE FLIGHT AIRPORT HOUR AIRPORT HOUR HOUR;
%%
#include "lex.yy.c"

int main(void) {
    yyparse();
    return 0;
}
