%{
#include <stdio.h>

int yylex(void) ;
void yyerror(char *) ;
%}

%token DOSSIER DOSSIERID

%%
expression: DOSSIER DOSSIERID;
%%
#include "lex.yy.c"

int main(void) {
    yyparse();
    return 0;
}
