%{
#include <stdio.h>
int yylex(void) ;
void yyerror(char *) ;
%}

%token DOSSIER DOSSIERID FLIGHT AIRPORT DATE HOUR OWNER END

%%
billet : dossier nom vol;
dossier : DOSSIER DOSSIERID;
nom : OWNER ;
vol : DATE FLIGHT AIRPORT HOUR AIRPORT HOUR HOUR vol
    | END
    ;
%%
#include "lex.yy.c"

int main(void) {
    yyparse();
    return 0;
}
