%{
#include <stdio.h>
int yylex(void) ;
void yyerror(char *) ;
%}

%token DOSSIER DOSSIERID FLIGHT AIRPORT DATE HOUR OWNER END

%%
dossier : DOSSIER DOSSIERID nom ;
nom : OWNER vol ;
vol : DATE FLIGHT AIRPORT HOUR AIRPORT HOUR HOUR vol
    | END
    ;
%%
#include "lex.yy.c"

int main(void) {
    yyparse();
    return 0;
}
